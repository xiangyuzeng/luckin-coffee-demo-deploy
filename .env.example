# Database - MongoDB connection string (REQUIRED)
# Get a free cluster at https://www.mongodb.com/cloud/atlas
# 1. Create account -> Build a Database -> Free Shared -> Create Cluster
# 2. Set up DB user (username/password) and Network Access (allow 0.0.0.0/0)
# 3. Click Connect -> Connect your application -> copy the connection string
DATABASE_URL="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/luckin-coffee?retryWrites=true&w=majority"

# NextAuth (REQUIRED)
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=""
# Set to your production URL on Vercel (e.g., https://your-app.vercel.app)
NEXTAUTH_URL="http://localhost:3000"
NEXT_SERVER_URL="http://localhost:3000"

# Google OAuth (OPTIONAL - for Google sign-in)
# Get credentials at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Stripe (OPTIONAL - for payments)
# Get keys at https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=""
STRIPE_SIGNATURE_SECRET=""

# Resend (OPTIONAL - for emails)
# Get API key at https://resend.com
RESEND_API_KEY=""
